[service]
    temp = "${PF_TEMP_PATH}"

[server]
    bind_address = "${PF_WS_HOST}"
    bind_port = ${PF_WS_PORT}

[redis]
    host = "${PF_REDIS_HOST}"
    port = ${PF_REDIS_PORT}

[sentry]
    dsn = "${PF_SENTRY_DSN}"
